<a href="http://localhost:4567/memos/new">  追加  </a>

<%# メモのタイトルを全て表示する %>
<% unless @notes.nil? %> <%# データが存在しているのか確認 %>
  <% @notes.each do |note| %>
    <p><a href="http://localhost:4567/memos/<%= note['id'] %>">•<%= h(note["title"].empty? ? '[no_title]':note['title']) %></a></p>
  <% end %>
<% end %> 
